# Copy this to .env for local development
# Both services read these same vars

DASHBOARD_PASSWORD=your_password_here

# Local dev: use a relative path. Railway: set to /data (the volume mount point)
DATA_DIR=./data
